[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "dabbah666-runtime-public-face"
version = "0.1.0"
description = "Public technical face for DABBAH 666 governed execution runtime"
readme = "README.md"
requires-python = ">=3.10"
